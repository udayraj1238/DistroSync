"""
DistroSync Producer Package

The producer is the client-side component that connects to the broker
and submits tasks for processing. It includes built-in retry logic
with exponential backoff for handling load-shedding responses.
"""
